import type { ThemeProps } from "@radix-ui/themes";

export const defaultTheme: ThemeProps = {
  appearance: 'light',
  accentColor: 'blue',
  grayColor: 'slate',
  radius: 'medium',
  scaling: '100%',
}